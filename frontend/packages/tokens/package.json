{
  "name": "@mizan/tokens",
  "version": "0.0.0",
  "private": true,
  "description": "Design tokens (SPEC §21.2): default theme, tenant primary derivation, accessible workflow palette",
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./tokens.css": "./src/tokens.css"
  },
  "scripts": {
    "lint": "eslint .",
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "devDependencies": {
    "@mizan/config": "workspace:*",
    "typescript": "~5.9.3",
    "vitest": "^5.0.0"
  }
}
