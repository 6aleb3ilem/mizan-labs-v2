# Runbook — Change the name of a status

Admin console → Workflows → the kind → the active version → edit the FR/EN label of the
state. The semantic (system behaviour) is unchanged; no deployment; the change is audited.
